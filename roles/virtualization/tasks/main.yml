---
- block:
  - name: Install packages
    become: true
    pacman:
      name:
        - virt-manager
        - qemu
        - libvirt
        - ovmf
        - bridge-utils
        - barrier
    when: is_desktop == "yes"
